{
  "summary": "The session pivoted from the original quantization-bias root-cause design (GPTQ/AWQ 8/4-bit on Qwen2.5-7B / Llama-3.1-8B / Gemma-2-9B over BBQ/StereoSet/CrowS-Pairs/BOLD/PTBB, with WGS/FR/mediation) to an adaptive-depth/early-exit fairness study asking whether a depth-adaptive decoder is a fairness-neutral allocator. The pivot was forced by de-risking evidence: bitsandbytes/auto-gptq/gptqmodel/autoawq are all missing and VRAM is only 8.59GB, so the 7-9B + real-quantizer grid is unexecutable as designed (evi_c9d802a0). The headline early-exit cell was built and run on real WinoBias type1 (occupation-disjoint clean-pro -> unseen-conflict-anti) for two Qwen2.5 backbones.\n\nRESULT: the core mechanism is SUPPORTED and reproducible. Under a shared confidence gate (tau=0.60, seed 42) per-group mean exit-depth DIFFERS (0.5B WGS_depth clean 0.536 / conflict 0.658; 1.5B clean 0.184 / conflict 0.045), the per-group exit-accuracy ordering REVERSES between clean and conflict AND relative to the full-depth final-layer model (up to 0.25-0.38 gap), and per-group calibration is strongly asymmetric (fitted temp 0.4 vs 3.0 at 0.5B, 1.5 vs 3.0 at 1.5B; asymmetric ECE). So \"early exit is NOT fairness-neutral\" holds as a mechanism -> C1 supported.\n\nBUT the directional/universal claim is NON-PORTABLE: the favored group flips between backbones (0.5B favors she on clean / he on conflict; 1.5B the reverse), only 2 of the required >=3 backbones were measured (Qwen3/DeepSeek/BERT arms not run), a near-chance 0.5B base confounds the signal, and the scope is narrow (WinoBias type1 only, one occupation-disjoint split/seed, pronoun-gender groups, small Qwen family). Critically, an independent verifier (evi_e71d11c5) flagged a data/interpretation integrity problem: the worker's INTERPRETATION attributes the gate/confidence to \"training-free per-layer LM-head confidence\" and claims a \"confident-but-wrong\" collapse (conf 0.91-0.96, acc 0.18-0.38), but the results JSON `confidence`/`gate_sweep` blocks are computed from the TRAINED per-layer option-scoring heads (head-mode=trained) and contain conf 0.54-0.78 with exit-accuracy 0.46-0.71 — the 0.91-0.96 / 0.18-0.38 values appear in NO artifact. Net: mechanism positive, directional claim non-portable, base-model confound unresolved, and the written interpretation must be corrected before the result is trustworthy.",
  "failureAnalysis": "The result is only partially successful and carries a verification-integrity concern. Root causes, in order of importance:\n\n1) Data/interpretation integrity (highest priority). The worker's EVIDENCE/INTERPRETATION and multiple node texts state that (a) the reportable signal is the \"training-free per-layer LM-head confidence\" and (b) the design's trained per-layer heads were \"overconfident 0.91-0.96 yet below-chance accuracy 0.18-0.38 -> confident-but-wrong stereotype collapse.\" An independent verification shows the results JSON `confidence`/`gate_sweep` blocks are actually produced by the TRAINED per-layer option-scoring heads (head-mode=trained in run_experiment.py) and report mean two-option confidence 0.54-0.78 with exit-accuracy 0.46-0.71; no 0.91-0.96 / 0.18-0.38 value exists in any artifact. So the \"confident-but-wrong collapse\" and the \"training-free is the reportable signal\" framings are NOT supported by the worker's own numbers and appear to be a mis-attribution. This is the blocker to fix before the finding can be trusted; the headline mechanism still holds, but the written causality around it does not.\n\n2) Non-portability of the directional claim. The favored group FLIPS between backbones (0.5B: she on clean / he on conflict; 1.5B: he on clean / she on conflict), so the universal directional claim fails. Only 2 of the required >=3 backbones were measured, so the C4/RC4 cross-backbone \"supported\" rule is not met; the verdict is honestly a mixed / NON-PORTABLE scoped result, not an over-claim. The gate/temperature direction is itself group-dependent (from the minimal verifier), which is consistent with the group-conditional-gate prediction but rules out a single scalar fairness-neutral gate.\n\n3) Base-model-quality confound. Qwen2.5-0.5B is near-chance at full depth (0.51-0.58), so part of the observed group asymmetry reflects a weak base model, not the exit mechanism. Without a matched-accuracy slice this is not cleanly separable.\n\n4) Original design left untested. The pivot to early-exit is justified by hardware/software constraints, but it leaves the d3_04/d3_05 quantization-bias root-cause question essentially open — the only evidence on it is the minimal verifier's 8-bit null (asymmetry ~0.006, 0 flips). The design's full benchmark/metric suite (BBQ race/gender, BOLD gender, WinoBias type2) and metrics (WGS, per-group B_g, AD, flip-rate FR_g, CANC, entropy mediation) were never exercised on the early-exit paradigm. Note also that RTN (used in the minimal verifier) is more destructive than GPTQ/AWQ, so even a real quantizer target is expected to show a smaller effect than the RTN proxy.\n\n5) Narrow scope. One benchmark (WinoBias type1), one occupation-disjoint split/seed, pronoun-gender groups, small Qwen family only — the design's cleaner split protocol (category/domain-disjoint held-out: BBQ race/religion/disability, StereoSet race/gender/religion) was not adopted, only an occupation-disjoint clean->conflict split.",
  "explorationDirections": [
    "FIX THE DATA-INTERPRETATION INTEGRITY FIRST: recompute and correctly re-attribute the confidence/gate sweep to the TRAINED per-layer option-scoring heads (head-mode=trained). Reconcile the `confidence`/`gate_sweep` block values against every claimed number, then correct the EVIDENCE/INTERPRETATION text that labels the signal as 'training-free' and claims a 0.91-0.96 conf / 0.18-0.38 acc 'confident-but-wrong' collapse. Establish whether the trained heads are actually reasonably calibrated (0.54-0.78 conf vs 0.46-0.71 acc) and therefore whether they are the viable primary mechanism, or whether there is a real separate training-free diagnostic to report. No new empirical claims should be added until this is resolved.",
    "COMPLETE THE CROSS-BACKBONE REQUIREMENT: run the cached Qwen3-0.6B / Qwen3-1.7B / DeepSeek-R1-Distill-Qwen-1.5B arms (all <=1.7B and already cached) to reach >=3 backbones and properly test C4/RC4 portability. Optionally add a BERT-base encoder arm (FastBERT/DeeBERT-style) as an architecture-family contrast to separate 'decoder early-exit' from 'encoder early-exit'. Report the direction of the favored group and whether the exit-depth disparity (WGS_depth) and the clean->conflict reversal persist across all backbones.",
    "EXTEND BEYOND WinoBias type1: apply the adaptive-depth pipeline to the other cached real datasets — WinoBias type2, BBQ gender (2832) and race (3440) ambiguous+disambiguated items, and BOLD gender (7201) — and compute the design's FULL metric suite (WGS, per-group B_g, asymmetry AD, flip-rate FR_g, aggregate-vs-per-group cancellation CANC, token-level entropy mediation) rather than only exit-depth/accuracy. This gives the per-group/gate claims real benchmark breadth.",
    "IMPLEMENT THE ROOT-CAUSE COUNTER AS AN ACTUAL INTERVENTION: fit a group-conditional gate / per-group temperature scaling on the CLEAN subset (no bias metric reported) and deploy it on the held-out unseen-conflict set, testing whether it collapses the per-group exit-depth disparity. The minimal verifier showed the temperature direction is group-dependent, so test a per-group (two-parameter) temperature rather than a single scalar; quantify the reduction in WGS_depth and in the clean->conflict accuracy-reversal gap.",
    "STRENGTHEN THE SPLIT PROTOCOL TO THE DESIGN'S STANDARD: fit the calibrated components on clean non-conflictive instances and validate on category/domain/source-disjoint held-out conflictive benchmarks (BBQ race/religion/disability held out; StereoSet race/gender/religion held out; CrowS-Pairs non-age; BOLD gender/race/religion) instead of only the occupation-disjoint clean->conflict split, to test whether the exit gate generalizes to unseen bias categories.",
    "CONTROL THE BASE-MODEL-QUALITY CONFOUND: add a matched-accuracy slice (report per-group exit-accuracy at matched full-depth and a matched-accuracy full-depth baseline) so the fair-group gap is separated from the near-chance 0.5B base quality, and explicitly mark cells as non-certifiable where per-group sample size is too small for calibration.",
    "DECIDE THE FATE OF THE ORIGINAL QUANTIZATION-BIAS HYPOTHESIS: either (a) attempt to install a real reconstructive quantizer (auto-gptq/gptqmodel/autoawq are currently missing) to test the 7-9B GPTQ/AWQ grid at full fidelity, or (b) explicitly de-scope d3_04/d3_05 to the RTN-based minimal-verifier null at 8-bit (and the entropy-collapse confound at 4-bit) and state that the original root-cause question is open rather than resolved."
  ]
}