{
  "feasibility": "medium",
  "generalizability": "medium",
  "risks": [
    "GROUP-vs-DIFFICULTY CONFOUND (top internal-validity threat): anti/conflict instances are lexically/syntactically harder than clean ones; matched-accuracy slices + per-template controls only partially separate group from difficulty at this n, so a 'fairness' finding could actually be a 'difficulty' finding. Add a difficulty-matched cleaner subset for the headline cells.",
    "BACKBONE FAMILY/SCALE CONFINEMENT: all 5 cached backbones are small Qwen/DeepSeek decoders (0.5-1.7B). The Scout's cross-architecture sweep shows early-exit applicability is architecture/scale-dependent (dense>MoE/SSM; >20B base), so a 'no fairness gap' conclusion cannot transfer to frontier scale or to encoder/other families; limit any such claim to the small-decoder family.",
    "MODEL CURRENCY / NARROW FAMILY: used models (Qwen2.5-0.5/1.5B, Qwen3-0.6/1.7B, DeepSeek-R1-Distill-Qwen-1.5B) are recent and from the Scout, but the Scout's other prominent/new families — Gemma (flagged worst-calibrated on gender), LLaMA (dominant open-weight), BERT (encoder cross-mechanism) — are absent. The encoder arm is only 'fetched if network allows.'",
    "TRAINED-HEAD CONFIDENCE NOT CALIBRATED TO TRUE ACCURACY: even trained per-layer head confidence is not true confidence; interpreting confidence-gated exit-depth gaps as 'uncertainty/miscalibration' vs 'capacity' in RC2 needs reliability-calibrated confidence, or RC2 conflicts miscalibration with capacity.",
    "PREMISE COHERENCE / DEFERRED QUANTIZATION (d3): minimal verification tested quantization-bias (8-bit RTN null; 4-bit confounded, RTN more destructive than GPTQ/AWQ) while the design tests early-exit fairness and defers the quantizer claim. If the thesis still asserts 'quantization amplifies bias,' the 8-bit null is unresolved and real GPTQ/AWQ 8-bit would likely be <= the RTN effect; the amplification narrative should be reframed as an honest negative or the thesis made crisply 'early-exit fairness.'",
    "PER-GROUP SMALL n / NON-CERTIFIABILITY: after held-out occupation/domain splits and for under-sampled groups (WinoBias type2, BOLD actress/race), per-group tau/calibration may be non-falsifiable exactly where the mitigation is claimed; mark non-certifiable cells rather than imputing.",
    "MULTIPLICITY: 5 backbones x 3 datasets x 2 splits x many metrics, mitigated by pre-declared endpoints (C1-C3), BH-FDR q=0.05, bootstrap CIs, and worst-group-by-max-rule; a false-fairness-positive remains if the RC4 honest-negative rule is not strictly enforced."
  ],
  "failureDirections": [
    "HONEST-NEGATIVE PRIMARY: if per-group exit-depth gap ~0 across backbones (early-exit is fairness-neutral), report as a legitimate robustness/negative result (RC4) — do not spin. Pivot the question to 'when does compute-allocation fairness matter at all' or move to larger-scale backbones where adaptive depth has real headroom.",
    "GENERIC-QUALITY CONFOUND: if the exit-depth gap is driven by quality degradation (analogous to the 4-bit entropy collapse in the minimal verification), separate confidence-reliability from capacity via calibration-corrected matched-accuracy slices; if it persists, the exit-signal framing is wrong and the driver is a quality floor, not calibration — pivot to lossless exits (speculative/Medusa-style) or model-level fairness interventions instead of entropy gates.",
    "CROSS-FAMILY / SCALE FAILURE: if small Qwen/DeepSeek results don't generalise, extend the ladder to a >=6-7B model (Qwen3-4B/8B, LLaMA-3.1-8B) and to an encoder (BERT + PABEE/DeeBERT heads) on a larger/cloud run; the 8.6GB cap bounds extrapolation, so otherwise restrict all claims to the small-decoder family.",
    "QUANTIZER ARM (d3, deferred): to keep any compression-bias sub-claim, install bitsandbytes/gptqmodel and run REAL GPTQ/AWQ at 8-bit (the realistic production vehicle). Given the RTN 8-bit null and that RTN is more destructive than GPTQ/AWQ reconstruction, expect the real 8-bit effect to be <= observed — frame as an honest negative (relevant since 4-bit is becoming production-default) and add a quality-matched 4-bit comparison to separate bias from degradation.",
    "MITIGATION-CANNOT-FIX CEILING: if RC2 group-conditional tau/calibration fails to collapse the accuracy gap, test the capacity hypothesis (free temp/tau, measure residual per-group accuracy at matched exit-depth); if the ceiling persists, calibration only reallocates limited compute and cannot raise a quality floor — report 'calibration could not fix it' as a useful negative and pivot to training-side or post-hoc debiasing."
  ],
  "verdict": "revise"
}