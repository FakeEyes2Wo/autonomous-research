# Token-Level Entropy Reveals Demographic Disparities in Large Language Models

## Metadata

- id: d2_01
- year: unknown
- venue: arXiv preprint (cs.CL/cs.CV)
- url: https://arxiv.org/abs/2501.19337

## Frontier Context

- role: B
- direction: d2
- why latest: Foundation for d2 (Jan 2025). Establishes that the exit-driving signal (next-token entropy) varies by signaled demographic group.
- novelty: Shows a demographic disparity in the entropy of the token distribution itself, independent of explicit group labels, and shows probing methodology determines whether/which direction a disparity is detected.

## One-liner

LLM token-entropy is already differentiated by signaled race/gender, so a confidence-gated exit threshold may exit differently across groups.

## Key Finding

Black-associated names yield higher first-token entropy/diversity than White ones (uniformly signed across models), whereas explicit label probing is mostly null/reversed.

## Contributions

- (not provided)

## Methods

- (not provided)

## Experiments

- (not provided)

## Results

- (not provided)

## Weakness

Sentence-completion entropy only; not tied to any early-exit/adaptive-compute policy; small per-prompt effects; no calibration/ECE analysis; chat-format dependence complicates generalization.

## Limitations

- (not provided)

## Future Directions

- (not provided)

## Implications / Insights

If entropy gates early exit (CALM/PABEE-style), a single shared threshold will not yield equal exit-depth or equal true-error across groups — the d2 hypothesis.

## Relevance to Us

A-1 (core, foundational). The empirical premise for d2 and the signal behind d1's exit decision.

## Abstract

A name alone measurably reshapes a model's next-token distribution before any token is sampled. The authors measure full-vocabulary Shannon entropy of the next-token distribution across six open-weight model families on 5,760 sentence-completion prompts where race/gender are signaled only by a first name. Black-associated names co-occur with higher first-token entropy and more diverse continuations than White-associated names — directionally consistent in all six instruction-tuned models under shared raw-text input, all six base checkpoints, and (for output diversity) five of six models under native chat formatting — opposite to the homogeneity bias seen under explicit group labels. The gap persists under tokenization/frequency controls and on a frequency-matched subset; per-prompt effects are small (d=0.06-0.16) but uniformly signed (template-level paired d=0.66-1.08). Gender points the other way, additively with race. First-token entropy attenuates sharply under chat formatting, and explicit group-label probing is mostly null/reversed; a variance-matched comparison locates the output-diversity disparity in heterogeneity across name-conditioned continuations.
