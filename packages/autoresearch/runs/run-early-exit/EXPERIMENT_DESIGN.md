{
  "datasets": [
    "WinoBias (uclanlp/wino_bias; CACHED): type1/type2 × pro(stereotype-consistent/clean) & anti(stereotype-violating/conflict); 396 val+test each; gold referent from coreference_clusters; groups = pronoun gender {he,she} × occupation-stereotype gender {M,F}. pro/anti are TEMPLATE-MATCHED TWINS (same occupations, same verb frame, only the group-relevant pronoun flips) — verified: 377/396 occupation pairs matched, e.g. anti 'The developer argued with the designer because she...' ↔ pro '...because he...'. This is a built-in difficulty control.",
    "BBQ (preferred public source heegyu/bbq — clean=context_condition='disambig', conflict=context_condition='ambig' with gold='Not enough information', biased pick=stereotyped_answer; in-cache guaranteed fallback hirundo-io/bbq-{gender,race,nationality}-bias-multi-choice which are ALL-ambiguous conflict sets, gender 2832/race 3440/nationality 1540). Groups = demographic attribute (gender identity, race/ethnicity, nationality). Real human-written ambiguous/disambiguated multi-choice questions.",
    "BOLD (AmazonScience/bold; CACHED, split='train'): real demographic bios. Clean = stereotype-consistent real bios; conflict = stereotype-violating real bios (person's real gender/race from the category label is opposite the occupation's stereotype in the same held-out domain). Groups = gender (American_actors vs American_actresses), race, profession. Generation task scored from the model-under-test's OWN generation (Regard/stereotype-alignment), never an external target model."
  ],
  "conflictConstruction": "ALL reported conflictive instances are the benchmark's own REAL text; NO synthetic occupation×pronoun×verb or name grid contributes to any reported metric. (1) WinoBias: the real type{1,2}_anti ('not_stereotype') sentences ARE the conflictive instances — the pronoun gender explicitly conflicts with the occupation's stereotypical gender (male-stereotyped 'developer' with 'she'); real type{1,2}_pro ('stereotype') sentences are clean; gold referent read from coreference_clusters. (2) BBQ: real ambiguous multi-choice items (correct_answer ∈ {'Not enough information','Cannot be determined','Not answerable','Unknown',...}; biased pick = stereotyped_answer) are the conflict set; real disambiguated stereotype-consistent items are clean; group = the demographic attribute under test. (3) BOLD: conflict = real bios where the person's real gender (from BOLD's gender-category split, e.g. American_actors vs American_actresses) is in an occupation stereotypically OPPOSITE that gender; clean = stereotype-consistent real bios; the bias state is scored from the model-under-test's own generation over the real prefix. Every conflictive instance carries a clean twin (WinoBias) or a difficulty-matched clean counterpart (BBQ/BOLD), so group-vs-difficulty is controllable. Difficulty-matched control: per-instance difficulty d(x) is computed on a FIXED-CAPACITY reference (base model's last-layer head, no early exit) as normalized NLL of the gold token(s) plus surface controls (length, distractor count, word frequency) — independent of the exit mechanism and group signal. WinoBias records d(x) per twin and reports Δ_KS(d); BBQ/BOLD select a difficulty-matched clean subset C* whose d(x) distribution matches the conflict pool (nearest-neighbour in (NLL,length) or matched deciles). Difficulty balance is diagnosed with a KS statistic / histogram overlap; if balance is poor (KS rejects α=0.05) the cell is DOWNGRADED and labelled difficulty-confounded, not imputed.",
  "splitProtocol": "Realistic CLEAN-train → UNSEEN-CONFLICT-test, split by REAL source/time/domain/occupation/category — NEVER random shuffle. All fitted components (per-layer exit heads, shared gate τ, per-group temperature/calibration) are fit ONLY on REAL CLEAN instances and validated on HELD-OUT CONFLICTIVE instances disjoint in source/domain/occupation/category. WinoBias: fit on type{1,2}_pro occupations in subset S; test on type{1,2}_anti for a HELD-OUT occupation subset S' (novel occupations at deployment) + within-domain pro→anti. BBQ: calibrate on clean gender_identity+nationality (disambiguated, non-cue); test on conflictive HELD-OUT category = race_ethnicity (unseen demographic domain) + ambiguous gender_identity/nationality (same source). BOLD: clean stereotype-consistent bios in domains D; test on stereotype-violating bios in HELD-OUT domains D'. Worst-group is FIXED by the pre-declared max rule over the declared group list, NEVER chosen post hoc. Clean and conflict sets are DISJOINT in category/domain/occupation. Matched-difficulty and matched-accuracy slices: within difficulty-matched subsets, further slice to equal per-group accuracy so any residual gap is measured at fixed difficulty AND fixed accuracy, separating group from both difficulty and quality.",
  "backbones": [
    "MAIN — 5 modern decoder backbones, all from the Model Scout, all cached, all runnable fp16 under 8.6GB (cross-backbone ≥3 requirement met with margin):",
    "  M1 Qwen/Qwen2.5-0.5B-Instruct (Alibaba 2024, L=24)",
    "  M2 Qwen/Qwen2.5-1.5B-Instruct (Alibaba 2024, L=28; scale-dependence axis)",
    "  M3 Qwen/Qwen3-0.6B (Alibaba 2025-26, L=28; scout frontier)",
    "  M4 Qwen/Qwen3-1.7B (Alibaba 2025-26, L=28; scout frontier)",
    "  M5 deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B (2025-26, L=28; scout DeepSeek reasoning variant)",
    "ENCODER CROSS-MECHANISM (secondary/validation baseline; fetchable, network up): google-bert/bert-base-uncased with per-layer PABEE/DeeBERT exit heads — CLASSIC early-exit encoder, baseline not main; fetch failure → decoder-only scope limit.",
    "CLASSIC BASELINES (non-main, only if fetchable & fits fp16): GPT-2 (CALM per-token entropy-gate baseline), T5 (calibration-machinery baseline)."
  ],
  "metrics": [
    "Exit-depth: per-group mean/median exit depth and distribution; per-group accuracy-vs-depth curve and AUC; between-group exit-depth gap (WGS_depth = max_g |mean_exit_depth_g − mean_exit_depth_overall|).",
    "Accuracy: per-group task accuracy (WinoBias coreference, BBQ multi-choice answer selection, BOLD generation) and accuracy-conditional-on-exited-depth slices; accuracy-vs-depth AUC.",
    "Bias: per-group stereotype/regard (BOLD), biased-pick rate (BBQ stereotyped_answer), pro-vs-anti stereotype gap (WinoBias); group-asymmetric gap and WGS_bias = max_g |Δbias_g|; aggregate-vs-per-group cancellation diagnostic (aggregate bias hiding opposite per-group shifts).",
    "Shift: Δ(exit-depth gap) and Δ(bias gap) CLEAN→CONFLICT per group; matched-accuracy slice to separate a genuine bias shift from generic degradation.",
    "Calibration: per-group ECE/Brier of the per-layer-head confidence and the CALM per-token entropy gate; per-group reliability curves; ECE before/after per-group temperature scaling; calibration-adjusted (reliability-calibrated) exit-depth gap.",
    "Mechanism/compute: mean layers/tokens before exit, % early-exit, per-layer head calibration, task quality/PPL (quality-degradation confound monitor), and the d(x) difficulty score + KS balance diagnostic (matched-difficulty control)."
  ],
  "rootCauseValidation": "Cross-backbone root-cause validation across the 5 cached modern decoder backbones (≥3 required, met with margin) on WinoBias/BBQ/BOLD, with an optional encoder cross-mechanism arm (BERT-base + PABEE/DeeBERT heads). Per (dataset, backbone), run the depth-adaptive decoder (per-layer heads trained on CLEAN only, frozen base) under a SHARED gate τ and a group-conditional calibrated variant. RC1 (descriptive, anti-cherry-pick): full factorial (backbone × dataset × group × split); per-group exit-depth, accuracy-vs-depth, bias, exit-depth gap, WGS, per-group ECE/Brier; Benjamini-Hochberg FDR q=0.05 within each dataset family; effect sizes + 95% bootstrap CIs; ALL cells reported. RC2 (root cause, calibration vs capacity — on RELIABILITY-CALIBRATED confidence): fit a single per-group temperature scalar on CLEAN per-layer-head logits so the exit maps to equal per-group reliability (equal true-error across groups); then fit per-group τ_g (CLEAN only) to EQUALIZE per-group accuracy on the CONFLICT test. Residual collapses to ~0 → root cause = group-dependent miscalibration (calibration sufficient); persists at matched accuracy AND matched difficulty AND matched reliability → group-specific capacity ceiling (calibration cannot raise a quality floor). Regime reported per (dataset, backbone) cell. RC3 (mediation): per-instance exit-depth-error E_i ~ group + reliability-calibrated confidence + accuracy (strata + bootstrap CIs); report the group direct path β_G vs the confidence-mediated share of between-group variance (ΔR²) to test the 'shared uncertainty signal' mechanism. RC4 (portability / HONEST NEGATIVE): pre-declared rule — 'root cause = group-dependent miscalibration' is SUPPORTED iff RC2 collapses in ≥2/3 of (dataset,backbone) cells AND no cell is significantly opposite-sign; else NON-PORTABLE. Enforced honest negatives: (a) per-group exit-depth gap ≈0 across backbones → report that early-exit is fairness-neutral as a legitimate robustness/negative result (never spin); (b) gap driven by quality degradation (analogous to the 4-bit entropy collapse under RTN) → separate confidence-reliability from capacity, report a quality-floor driver, pivot to lossless/speculative exits or model-level interventions; (c) small Qwen/DeepSeek results don't transfer to a larger model or encoder → restrict all claims to the small-decoder family. Multiplicity control: pre-registered primary endpoints (C1-C4), BH-FDR q=0.05, bootstrap CIs, worst-group by pre-declared max rule, fixed denominators, 3 sensitivity seeds {42,123,7} on the headline, NO tuning on the conflict/eval set (τ, heads, calibration fit on CLEAN only), NO result-conditional subsetting.",
  "limitations": [
    "HARDWARE: 8.6GB VRAM caps the in-memory scale axis at ≤1.7B fp16; ≥3-4B and 7-70B require downloads that may fail or OOM on this GPU. Scale axis covers 0.5-1.7B only; any claim is restricted to the small-decoder family.",
    "MODEL FAMILY/SCOPE: all 5 main backbones are small Qwen/DeepSeek decoders; the Scout's cross-architecture sweep shows adaptive-depth applicability is architecture/scale-dependent (dense>MoE/SSM, >20B base), so a 'no fairness gap' result cannot transfer to frontier scale or MoE/SSM families. The encoder arm (BERT-base + PABEE/DeeBERT) is a classic baseline and fetch-dependent; without it generalization is decoder-only.",
    "MODEL CURRENCY / ABSENT FAMILIES: Gemma (flagged worst-calibrated on gender) and LLaMA (dominant open-weight) are not cached and cannot fit on this GPU; flagged for a cloud run. Main ladder = recent Qwen/DeepSeek (both Scout-listed).",
    "QUANTIZATION / d3 TRANSFER (deferred, honest negative): bitsandbytes/auto-gptq/gptqmodel/autoawq all absent (verified), so GPTQ/AWQ cannot run; naive RTN is a destructive proxy whose 8-bit effect was already fairness-null in the minimal verifier. The 'compression amplifies bias' narrative is an HONEST-NEGATIVE descriptor only; the active thesis is exclusively early-exit fairness.",
    "TRAINED-HEAD CONFIDENCE IS NOT TRUE CONFIDENCE: per-layer head confidence (even temperature-scaled) is a reliability proxy; RC2's miscalibration-vs-capacity verdict depends on per-group scaling actually equalizing reliability. Cells where reliability cannot be matched are correctly labelled 'reliability-unmatched', not imputed.",
    "PER-GROUP SMALL n / NON-CERTIFIABILITY: after held-out occupation/category/domain splits and for under-sampled groups (WinoBias type2, BOLD actress/race subsets, BBQ held-out category), per-group τ/calibration can be uncertifiable exactly where the mitigation is claimed; report per-group n + 95% bootstrap CIs and mark under-sampled cells NON-CERTIFIABLE, not imputed.",
    "COMPARABILITY: BOLD Regard, BBQ biased-pick rate, and WinoBias coreference accuracy are DIFFERENT bias-state definitions; report per-measure-per-dataset with sign-consistency across datasets and do NOT sum a single cross-dataset WGS/FDR.",
    "SHIFT COVERAGE: 'unseen-conflict' = held-out category/domain/occupation (realistic, not exhaustive of adversarial or temporally-drifting stereotype inversion).",
    "GENERATION-METRIC BOUNDEDNESS: BOLD Regard/stereotype-alignment is a finite lexical heuristic that can under/over-report bias; mitigate with multiple bias-state measures and the aggregate-vs-per-group cancellation diagnostic.",
    "GROUP-vs-TEXT-DIFFICULTY: controlled via template-matched twins + a difficulty-matched clean subset C* + matched-difficulty AND matched-accuracy slices, but not eliminated for every cell; poor-balance cells are downgraded and reported, not hidden.",
    "MULTIPLICITY: 5 backbones × 3 datasets × 2 splits × several metrics is a large factorial; pre-declared endpoints, BH-FDR q=0.05, bootstrap CIs, and worst-group-by-max-rule mitigate it, but a false-fairness-positive remains if the RC4 honest-negative rule is not strictly enforced."
  ]
}