# Experiments

This directory contains reproducible experiment workspaces and validation artifacts.

- [`figure-validation/`](figure-validation/) — the figure pipeline, including its inputs, scripts, and generated validation outputs.

Experiment scripts resolve their own data relative to `__file__`; run commands from the repository root as documented by each experiment.
