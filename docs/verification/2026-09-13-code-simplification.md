# 2026-09-13 代码简化验收

本轮在当前主工作区完成行为保持的简化，由三个实现子进程编写代码和回归测试，主进程负责协调、文档和验证。此前的目录整理与用户修改保留；独立的 `feat/evidence-driven-research` 工作树未合并、未修改。本轮代码尚未提交 Git。

## 简化结果

| 范围 | 收敛的重复逻辑 |
|---|---|
| 研究流程 | 实验终态持久化与完成导出、文献 insight 字段清洗、论文 sections 文件写入 |
| 配置与调用 | 原生调用和 JSON 修复的公共生命周期、设置原子提交、请求结算；移除纯转发函数、重复导出和未使用导入 |
| Web/CPA | 八处项目与四处文档 guard 的公共校验、路由选项键集合、两个 CLI 的参数与配置读取 |

保持公开接口、存储格式、错误映射、预算和恢复边界；未新增依赖或通用运行框架。领域差异仍留在调用点，例如 repair 工具限制、设置锁与版本冲突检查、论文 bibliography 写入顺序，以及 uninstall 不读取配置。

统计仅覆盖本轮变更的 20 个生产源文件，排除测试、文档、构建输出。已有文件以 Git HEAD 为基线，新 CLI 文件以零为基线；两侧统一为 LF 后计算 UTF-8 字节和行数，避免换行转换污染结果。

| 生产源码组 | UTF-8 字节变化 | 行数变化 |
|---|---:|---:|
| AutoResearch 核心 | 190,410 → 187,217（−3,193） | 4,156 → 4,135（−21） |
| Web workbench | 35,670 → 34,967（−703） | 616 → 626（+10） |
| CPA | 14,850 → 14,509（−341） | 259 → 252（−7） |
| **合计** | **240,930 → 236,693（−4,237）** | **5,031 → 5,013（−18）** |

部分公共函数采用更清晰的换行和具名选项，因此并非每个文件都减少行数。收益是重复实现和无用绑定减少；没有通过删测试、删保护或压缩排版制造减量，也不宣称已经达到项目代码的绝对最小规模。

## 验证和审查

- 基线：核心 155、Web 60、CPA 13，共 228 项测试通过。
- 最终：核心 157、Web 60、CPA 13，共 **230 项测试通过**，失败与跳过均为 0。
- 核心构建及 TypeScript 检查通过，包含额外的 `--noUnusedLocals --noUnusedParameters` 检查；Web 构建、CPA typecheck/build 通过。
- 独立审查对三组变更均批准。审查发现的两个行为漂移已修复，并新增回归：无 sessionId 的早期 repair 流式事件保留原临时记账 ID；section 写失败时保持 `main.tex → references.bib → sections` 的部分落盘顺序。
- 原生调用资源释放、设置冲突、结算幂等、Web 路径保护与错误映射通过相关现有测试及审查。CLI smoke 使用受控临时目录，没有访问真实凭据或启动在线探测。

最终测试日志、规范化度量与独立审查记录保存在 `.runs/simplification/`。这些是本地软件验证证据，不是科研实验结论。

## 详细记录

- [总体实施计划](../superpowers/plans/2026-09-13-code-simplification.md)
- [研究流程简化](2026-09-13-simplify-core.md)
- [配置与调用边界简化](2026-09-13-simplify-boundary.md)
- [Web/CPA 简化](2026-09-13-simplify-web-cpa.md)

各子任务报告保留其验证时间点的信息；本页的最终测试数和统一 LF 度量为本轮整体验收结果。
